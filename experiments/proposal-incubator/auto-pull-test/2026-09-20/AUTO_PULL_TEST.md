# Obsidian Auto Pull Test — 2026-09-20

Purpose: verify that Obsidian Git automatically pulls new files from the AI-Harness development branch without manual `git pull`.

Expected behavior:
- Obsidian Git is configured with Pull on startup = ON.
- Auto pull interval = 10 minutes.
- Auto commit = OFF.
- Auto push = OFF.
- This file should appear locally in Obsidian without manual Git commands.

Status: waiting for local observation.
